// Signal Breakdown Card Component
//
// One card per signal (feed, temperature, mortality).
// Shows: current value, baseline, deviation percentage, trend arrow (up/down/stable).
// Colour-coded by deviation severity.
