// "Without GLM" Toggle Component
//
// Switch that toggles between "with GLM" and "without GLM" views.
// Used during demo to show judges that removing GLM degrades
// the system to raw numbers without actionable meaning.
