// Action List Component
//
// Renders GLM-recommended actions as a prioritised checklist.
// Each action shows: priority number, action text, timeframe, status.
// Farmer can tap to mark actions as "done" (feeds into feedback loop).
