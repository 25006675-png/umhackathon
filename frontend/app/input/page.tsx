// Daily Input Form Page (Layer 1 — Data Collection)
//
// Mobile-optimised form fields:
// - Shed temperature (°C) — number input
// - Feed intake (kg) — number input
// - Mortality count — number input
// - Farmer notes — free text area (supports Malay and English)
//
// On submit: POST to /api/readings, then redirect to dashboard
// Design: large touch targets, minimal text, clear labels in Malay
