# GLM Routes — Section 3 (Z.AI GLM Intelligence)
#
# POST /api/glm/analyse/{flock_id}     — full GLM analysis chain (interpretation → diagnosis → recommendations)
# POST /api/glm/chat                   — conversational follow-up (stretch goal — bilingual)
# GET  /api/glm/compare/{flock_id}     — side-by-side "with GLM" vs "without GLM" for demo
