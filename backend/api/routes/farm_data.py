# Farm Data Routes — Layer 1 Input
#
# POST /api/readings       — submit a daily reading (temp, feed, mortality, notes)
# GET  /api/readings/{id}  — get a specific reading
# GET  /api/flocks/{id}/readings — get reading history for a flock
# POST /api/flocks          — register a new flock
